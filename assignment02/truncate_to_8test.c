/*
Compile: make wages
Run: ./wages
*/
 
#include "base.h"
#include "string.h"

/*
Design a function that computes weekly wages with overtime from hours worked. The hourly rate is 10 €/hour. Regular working time is 40 hours/week. Overtime is paid 150% of the normal rate of pay.
*/
 
 int grenze=8; // represents hours worked

 
// Hours -> Cents
//s_length(String s) hours_to_wages(len len);
 
/*void hours_to_wages_test() {
   // check_expect_s(s_sub(string,1,len),"12345678" );          // line 20

}
*/
// Compute the wage in cents given the number of hours worked.
/*s_length(String s) hours_to_wages(int grenze) {
    if (grenze <= s_length(String s)) {
        return s_length(String s)=grenze;
    } else {
        return s_length(String s);
    }
}
*/
int main(void) {
	String s= s_input(100);
	int s_length(String s);

   if (s_length(s) <= 8) {
	printiln(69);
	} else {
	printiln(s_sub(5);
	}
	return 0;
	}
